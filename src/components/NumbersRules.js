import React from 'react';

const NumbersRules = () => {
  return (
    <ul className="NumbersRules">
      <li>6 Numbers Small (1-10) and Big (25,50,75,100)</li>
      <li>Generate a target & Start the clock for a 30 second timer</li>
      <li>Each number can only be used once</li>
      <li>Only the (+ - * /) mathematical operations are allowed;</li>
      <li>10 points for correct answers only</li>
    </ul>
  );
};

export default NumbersRules;
