import React from 'react';

const LettersRules = () => {
  return (
    <ul className="LettersRules">
      <li>Select a mix of 9 consonants or vowels</li>
      <li>Start the clock for a 30 second timer</li>
      <li>
        Type your word into the text field to update the scores at the top
      </li>
      <li>Points go to the player with the longest word</li>
    </ul>
  );
};

export default LettersRules;
